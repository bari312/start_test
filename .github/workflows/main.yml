name: CI/CD with Trading Environment
on: [push, pull_request]

jobs:
  build-and-test:  
    runs-on: ubuntu-latest  
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Just print a message
        run: echo "YAML syntax is correct."
     
